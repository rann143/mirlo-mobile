import { Text, View, Image, FlatList, SafeAreaView } from "react-native";
import { Stack } from "expo-router";

export default function AlbumTracks() {
  return (
    <SafeAreaView>
      <View>
        <Text>Album Tracks</Text>
      </View>
    </SafeAreaView>
  );
}
